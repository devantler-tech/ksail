name: "TODOs"

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  issues: write

jobs:
  todos:
    uses: devantler-tech/reusable-workflows/.github/workflows/todos.yaml@0f7b26323a6983141526673207d381445851aec3 # v1.29.1
    secrets:
      APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}
