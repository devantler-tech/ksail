name: "TODOs"

permissions:
  contents: write
  issues: write
  pull-requests: write

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  todos:
    uses: devantler-tech/reusable-workflows/.github/workflows/todos.yaml@920ee277bb1932a1c4f29e9d97da54181b43e79e # v1.19.3
    secrets:
      APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}
