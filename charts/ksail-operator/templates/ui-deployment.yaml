{{- if .Values.ui.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ksail-operator.fullname" . }}-ui
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ksail-operator.labels" . | nindent 4 }}
    app.kubernetes.io/component: ui
spec:
  replicas: {{ .Values.ui.replicas }}
  selector:
    matchLabels:
      {{- include "ksail-operator.uiSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "ksail-operator.uiSelectorLabels" . | nindent 8 }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      containers:
        - name: ui
          image: {{ include "ksail-operator.uiImage" . | quote }}
          imagePullPolicy: {{ .Values.ui.image.pullPolicy }}
          env:
            # The single read-only lock value, shared with the operator API.
            - name: UI_READ_ONLY
              value: {{ .Values.ui.readOnly | quote }}
            - name: API_BASE_URL
              value: "http://{{ include "ksail-operator.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.api.bindPort }}"
          ports:
            - name: http
              containerPort: 80
          resources:
            {{- toYaml .Values.ui.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
