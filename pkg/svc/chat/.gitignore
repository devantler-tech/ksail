# The docs/ directory is no longer needed — documentation is now generated
# into docs_generated.go by gen_docs.go at go:generate time.
# Ignore the docs/ directory in case it gets recreated accidentally.
docs/
