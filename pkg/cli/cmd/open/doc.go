// Package open implements the `ksail open` command group, which opens KSail's interfaces for
// provisioning and managing clusters: the web UI (`ksail open web`), the native desktop app
// (`ksail open desktop`), the AI chat assistant (`ksail open chat`), and the MCP server
// (`ksail open mcp`).
package open
