name: "TODOs"

permissions:
  contents: write
  issues: write
  pull-requests: write

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  todos:
    uses: devantler-tech/reusable-workflows/.github/workflows/todos.yaml@e5c7943bc0eaea81d859415319d9f5c17d1470e9 # v1.6.1
    secrets:
      APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}
