// Package vclusterprovisioner implements the cluster provisioner interface for
// vCluster's Docker driver (Vind). It uses the vCluster Go SDK for Create and
// Delete operations and delegates Start/Stop/List/Exists to the Docker
// infrastructure provider.
package vclusterprovisioner
